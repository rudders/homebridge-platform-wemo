{
  "pluginAlias": "BelkinWeMo",
  "pluginType": "platform",
  "headerDisplay": "Homebridge plugin to control Wemo devices.",
  "footerDisplay": "For help and support please visit our [GitHub Wiki](https://github.com/bwp91/homebridge-platform-wemo/wiki).",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "title": "Name",
        "description": "The plugin name that will show up in Homebridge logs.",
        "default": "Belkin WeMo",
        "required": true
      },
      "noMotionTimer": {
        "type": "integer",
        "title": "No Motion Timer",
        "description": "A timer (in seconds) for how long after motion is not detected that the state is changed.",
        "default": 60,
        "minimum": 0,
        "maximum": 300
      },
      "ignoredDevices": {
        "title": "Ignored Devices",
        "description": "You can use this optional setting to ignore (and remove) devices from Homebridge.",
        "type": "array",
        "items": {
          "type": "string",
          "title": "Serial Number",
          "description": "The device serial number you wish Homebridge to ignore."
        }
      },
      "manualDevices": {
        "title": "Manual Devices",
        "description": "You can use this optional setting to define devices that should be configured manually.",
        "type": "array",
        "items": {
          "type": "string",
          "title": "Device URL",
          "description": "Example 'http://192.168.1.20:49153/setup.xml'"
        }
      },
      "disableDiscovery": {
        "type": "boolean",
        "title": "Disable Device Discovery",
        "description": "If enabled, device auto-discovery will be skipped.",
        "default": false
      },
      "discoveryInterval": {
        "type": "integer",
        "title": "Discovery Interval",
        "description": "An interval (in seconds) in which SSDP-SEARCH broadcasts are sent. This setting will influence the time it takes to discover new devices.",
        "default": 30,
        "minimum": 30,
        "maximum": 600
      },
      "debug": {
        "title": "Debug Logging",
        "type": "boolean",
        "required": true,
        "description": "If enabled, more information will be added to the Homebridge log.",
        "default": false
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "Required Settings",
      "description": "These are the basic settings that are required for this plugin to work.",
      "items": [
        "name"
      ]
    },
    {
      "type": "fieldset",
      "title": "Optional Settings",
      "description": "A variety of optional settings for general use of this plugin.",
      "expandable": true,
      "items": [
        "debug",
        "disableDiscovery",
        "discoveryInterval",
        "noMotionTimer",
        {
          "type": "help",
          "helpvalue": "<label>Wemo Client</label><p class=\"help-block\">Please see <a href=\"https://github.com/bwp91/homebridge-platform-wemo/wiki/Configuration\" target=\"_blank\">the wiki</a> to manually configure this.</p>"
        }
      ]
    },
    {
      "key": "ignoredDevices",
      "expandable": true,
      "title": "Ignored Devices",
      "add": "Add Another Device",
      "type": "array",
      "items": [
        {
          "type": "fieldset",
          "items": [
            "ignoredDevices[]"
          ]
        }
      ]
    },
    {
      "key": "manualDevices",
      "expandable": true,
      "title": "Manual Devices",
      "add": "Add Another Device",
      "type": "array",
      "items": [
        {
          "type": "fieldset",
          "items": [
            "manualDevices[]"
          ]
        }
      ]
    }
  ]
}
